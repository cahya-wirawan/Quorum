import RepositoriesPage from "../../(dash)/repos/page";

export default function OnboardingReposPage() {
  return (
    <div style={{ padding: "var(--q-6)", maxWidth: "var(--q-max-content)", margin: "0 auto" }}>
      <RepositoriesPage />
    </div>
  );
}
